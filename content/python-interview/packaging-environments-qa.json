{
  "questions": [
    {
      "id": "py-pkg-import-system-and-sys-path",
      "level": "Middle",
      "prevalence": "Common",
      "category": "Modules, packaging and environments",
      "kind": "Theory",
      "question": "How does Python find a module when you write `import something`, and what role does `sys.path` play?",
      "shortAnswer": "On `import something`, Python first checks `sys.modules` to see if the module was already imported and cached; if not, it searches the directories listed in `sys.path`, in order, until it finds a matching module or package. `sys.path` starts with the directory of the running script (or the current directory), then entries from the `PYTHONPATH` environment variable, then the standard library and installed site-packages locations; whichever matching file is found first wins, which is exactly why a locally-named file that shadows a standard-library or installed module name (like `random.py` in your own project) can silently break unrelated imports.",
      "strongAnswerSignals": ["checks sys.modules cache first, then searches sys.path directories in order", "sys.path includes the script's directory, PYTHONPATH, stdlib, site-packages", "first match wins, so a local file can shadow a standard-library module name"],
      "questionUk": "Як Python знаходить модуль, коли ви пишете `import something`, і яку роль відіграє `sys.path`?",
      "shortAnswerUk": "При `import something` Python спершу перевіряє `sys.modules`, чи модуль уже імпортовано й закешовано; якщо ні — шукає в каталогах, перелічених у `sys.path`, по черзі, доки не знайде відповідний модуль чи пакет. `sys.path` починається з каталогу запущеного скрипта (чи поточного каталогу), потім записи зі змінної середовища `PYTHONPATH`, потім стандартна бібліотека й встановлені розташування site-packages; перемагає перший знайдений відповідний файл, саме тому локально названий файл, що затінює ім'я стандартної бібліотеки чи встановленого модуля (наприклад, `random.py` у власному проєкті), може тихо зламати непов'язані імпорти.",
      "strongAnswerSignalsUk": ["спершу перевіряє кеш sys.modules, потім шукає каталоги sys.path по черзі", "sys.path включає каталог скрипта, PYTHONPATH, stdlib, site-packages", "перемагає перший збіг, тож локальний файл може затінити стандартний модуль"],
      "example": "Creating a personal script named `json.py` in a project's root directory can silently break every later `import json` in that same directory, because the local file is found in `sys.path` before the standard library's `json` module.",
      "exampleUk": "Створення власного скрипта з іменем `json.py` в кореневому каталозі проєкту може тихо зламати кожен наступний `import json` у цьому ж каталозі, бо локальний файл знаходиться в `sys.path` раніше за модуль `json` стандартної бібліотеки.",
      "tags": ["imports", "modules"],
      "sourceIds": ["python-docs-import-system"]
    },
    {
      "id": "py-pkg-init-py-and-packages",
      "level": "Middle",
      "prevalence": "Common",
      "category": "Modules, packaging and environments",
      "kind": "Theory",
      "question": "What is the purpose of an `__init__.py` file, and is it still required to make a directory a package?",
      "shortAnswer": "`__init__.py` marks a directory as a regular package and runs automatically the first time any module inside that package is imported, making it the natural place to define what the package exposes at its top level, run package-wide setup, or explicitly control `from package import *` via `__all__`. Since Python 3.3 (PEP 420), a directory without `__init__.py` can still be imported as an implicit \"namespace package\", which is useful for splitting a single logical package across multiple distributed directories, but an explicit `__init__.py` remains the normal, more predictable choice for an ordinary application package.",
      "strongAnswerSignals": ["__init__.py runs on first import of anything inside the package", "natural place for top-level exports, setup, or __all__", "namespace packages (PEP 420, 3.3+) allow omitting it, but explicit is still typical"],
      "questionUk": "Яка мета файлу `__init__.py`, і чи все ще потрібен він, щоб перетворити каталог на пакет?",
      "shortAnswerUk": "`__init__.py` позначає каталог як звичайний пакет і автоматично виконується при першому імпорті будь-якого модуля всередині цього пакета, що робить його природним місцем для визначення того, що пакет виставляє на верхньому рівні, виконання налаштування на рівні всього пакета чи явного контролю `from package import *` через `__all__`. З Python 3.3 (PEP 420) каталог без `__init__.py` все ще можна імпортувати як неявний «простір-імен пакет» (namespace package), що корисно для розділення однієї логічної пакетної структури на кілька розподілених каталогів, але явний `__init__.py` залишається звичайним, більш передбачуваним вибором для звичайного пакета застосунку.",
      "strongAnswerSignalsUk": ["__init__.py виконується при першому імпорті чогось усередині пакета", "природне місце для верхньорівневих експортів, налаштування чи __all__", "простір-імен пакети (PEP 420, 3.3+) дозволяють опустити його, але явний варіант типовий"],
      "example": "`mypackage/__init__.py` containing `from .core import main_function` lets callers write `from mypackage import main_function` instead of the deeper `from mypackage.core import main_function`.",
      "exampleUk": "`mypackage/__init__.py` з вмістом `from .core import main_function` дозволяє викликачам писати `from mypackage import main_function` замість глибшого `from mypackage.core import main_function`.",
      "tags": ["packages", "modules"],
      "sourceIds": ["python-docs-import-system", "python-docs-tutorial"]
    },
    {
      "id": "py-pkg-venv-isolation",
      "level": "Junior",
      "prevalence": "Very common",
      "category": "Modules, packaging and environments",
      "kind": "Practical",
      "question": "Why does every Python project need its own virtual environment instead of installing packages globally?",
      "shortAnswer": "A virtual environment, created with `python -m venv .venv`, gives a project its own isolated Python interpreter and `site-packages` directory, so its dependencies and their exact versions do not conflict with another project's dependencies or with packages installed system-wide. Without isolation, two projects that need incompatible versions of the same library cannot both work on one machine, and upgrading a dependency for one project risks silently breaking an unrelated one that happened to share the global environment.",
      "strongAnswerSignals": ["venv creates an isolated interpreter and site-packages per project", "avoids version conflicts between unrelated projects", "without it, upgrading a shared global dependency can break other projects"],
      "questionUk": "Чому кожен проєкт Python потребує власного віртуального середовища, а не глобального встановлення пакетів?",
      "shortAnswerUk": "Віртуальне середовище, створене через `python -m venv .venv`, надає проєкту власний ізольований інтерпретатор Python і каталог `site-packages`, тож його залежності та їхні точні версії не конфліктують із залежностями іншого проєкту чи з пакетами, встановленими глобально в системі. Без ізоляції два проєкти, яким потрібні несумісні версії однієї бібліотеки, не можуть обидва працювати на одній машині, а оновлення залежності для одного проєкту ризикує тихо зламати непов'язаний проєкт, що випадково поділяв те саме глобальне середовище.",
      "strongAnswerSignalsUk": ["venv створює ізольований інтерпретатор і site-packages для проєкту", "уникає конфліктів версій між непов'язаними проєктами", "без ізоляції оновлення спільної глобальної залежності може зламати інші проєкти"],
      "example": "Project A needs `requests==2.20` and project B needs `requests==2.32`; without separate virtual environments, installing one version globally breaks whichever project needed the other.",
      "exampleUk": "Проєкту A потрібен `requests==2.20`, а проєкту B — `requests==2.32`; без окремих віртуальних середовищ встановлення однієї версії глобально ламає той проєкт, якому була потрібна інша.",
      "tags": ["venv", "environments"],
      "sourceIds": ["python-docs-venv", "packaging-guide"]
    },
    {
      "id": "py-pkg-pip-requirements-and-lockfiles",
      "level": "Middle",
      "prevalence": "Common",
      "category": "Modules, packaging and environments",
      "kind": "Practical",
      "question": "What is the difference between a loosely-pinned `requirements.txt` and a fully-locked dependency file, and why does reproducibility matter?",
      "shortAnswer": "A loosely-pinned requirements file (`requests>=2.20`) only names direct dependencies and leaves their exact resolved versions, and every transitive dependency's version, up to whatever `pip` happens to resolve at install time, which can differ between two installs done weeks apart as new versions are published. A fully-locked file pins every package, direct and transitive, to an exact version (and often a content hash), which guarantees the same declared artifacts install consistently within the environments that lock file was actually built for — a given Python version, OS and architecture, with the locked source or wheel artifacts still available. It does not by itself guarantee an identical result on every machine or forever into the future: platform-specific wheels, environment markers, and artifacts that later become unavailable from an index can still make outcomes vary across genuinely different environments or across time. `pylock.toml` (PEP 751) is the current standardized, tool-agnostic lock file format built specifically for reproducible installs within a declared set of compatible environments. Reproducibility within those bounds is still essential for consistent builds, debugging \"works on my machine\" issues, and safe rollbacks.",
      "strongAnswerSignals": ["loose pins leave exact resolved and transitive versions up to install-time resolution", "a lock file pins every package to an exact version/hash, reproducible within its declared compatible environments — not an unconditional forever-and-anywhere guarantee", "pylock.toml (PEP 751) is the current standardized lock file format for this"],
      "questionUk": "Яка різниця між слабко зафіксованим `requirements.txt` і повністю заблокованим файлом залежностей, і чому важлива відтворюваність?",
      "shortAnswerUk": "Слабко зафіксований файл вимог (`requests>=2.20`) називає лише прямі залежності й залишає точні розв'язані версії, а також версію кожної транзитивної залежності, на розсуд того, що `pip` розв'яже на момент встановлення, що може відрізнятися між двома встановленнями, зробленими з різницею в тижні, коли публікуються нові версії. Повністю заблокований файл фіксує кожен пакет, прямий і транзитивний, до точної версії (і часто хешу вмісту), що гарантує послідовне встановлення тих самих задекларованих артефактів у межах середовищ, для яких цей файл блокування справді був побудований — конкретна версія Python, ОС і архітектура, з доступними заблокованими артефактами джерела чи wheel. Сам по собі він не гарантує ідентичного результату на будь-якій машині чи назавжди в майбутньому: специфічні для платформи wheel, маркери середовища й артефакти, що пізніше стають недоступними в індексі, все ще можуть давати різні результати між справді різними середовищами чи з часом. `pylock.toml` (PEP 751) — поточний стандартизований, незалежний від інструменту формат файлу блокування, створений саме для відтворюваних встановлень у межах задекларованого набору сумісних середовищ. Відтворюваність у цих межах все ще необхідна для стабільних збірок, налагодження проблем «у мене працює» й безпечних відкатів.",
      "strongAnswerSignalsUk": ["слабка фіксація залишає точні й транзитивні версії на розсуд розв'язання при встановленні", "файл блокування фіксує кожен пакет до точної версії/хешу, відтворюваний у межах задекларованих сумісних середовищ — не безумовна гарантія назавжди й будь-де", "pylock.toml (PEP 751) — поточний стандартизований формат файлу блокування для цього"],
      "example": "A CI pipeline that installs from a loose `requirements.txt` can start failing overnight when an unrelated transitive dependency ships a breaking change, while a locked file keeps builds stable until someone deliberately updates it.",
      "exampleUk": "Конвеєр CI, що встановлює зі слабко зафіксованого `requirements.txt`, може почати падати за ніч, коли непов'язана транзитивна залежність випустить зворотно несумісну зміну, тоді як заблокований файл тримає збірки стабільними, доки хтось навмисно його не оновить.",
      "tags": ["packaging", "dependencies"],
      "sourceIds": ["packaging-guide"]
    },
    {
      "id": "py-pkg-pyproject-toml",
      "level": "Middle",
      "prevalence": "Common",
      "category": "Modules, packaging and environments",
      "kind": "Theory",
      "question": "What is `pyproject.toml`, and what did it replace in modern Python packaging?",
      "shortAnswer": "`pyproject.toml` is the standardized, tool-agnostic configuration file (PEP 518 for the build-system table, PEP 621 for project metadata) that a Python project uses to declare its build backend, dependencies, name, version and other metadata in one place. It is the modern standard surface for this, largely replacing the older pattern of a hand-written `setup.py` script plus scattered `setup.cfg` sections as the primary place project metadata lives — but `setup.py` and Setuptools themselves are not deprecated; what the Python Packaging Authority has specifically deprecated is invoking `setup.py` directly as a command (`python setup.py install`, `python setup.py sdist`), in favor of standardized build-frontend tools that read `pyproject.toml`. Modern tools like linters, formatters and test runners also increasingly read their own configuration from `pyproject.toml`, and the standardized `[dependency-groups]` table (PEP 735) now lets a project declare groups of development-only dependencies — test, lint, docs — there as well.",
      "strongAnswerSignals": ["standardized config file for build system and project metadata (PEP 518/621)", "setup.py and Setuptools are not deprecated — only invoking setup.py directly as a command is", "[dependency-groups] (PEP 735) now lets pyproject.toml declare dev dependency groups too"],
      "questionUk": "Що таке `pyproject.toml`, і що він замінив у сучасному пакуванні Python?",
      "shortAnswerUk": "`pyproject.toml` — стандартизований, незалежний від інструменту файл конфігурації (PEP 518 для таблиці системи збирання, PEP 621 для метаданих проєкту), який проєкт Python використовує, щоб оголосити свій бекенд збирання, залежності, ім'я, версію та інші метадані в одному місці. Це сучасна стандартна поверхня для цього, що переважно замінює старіший підхід із написаним вручну скриптом `setup.py` плюс розрізненими секціями `setup.cfg` як основне місце для метаданих проєкту — але самі `setup.py` й Setuptools не застарілі; PyPA спеціально позначила застарілим саме пряме виконання `setup.py` як команди (`python setup.py install`, `python setup.py sdist`), на користь стандартизованих інструментів-фронтендів збирання, що читають `pyproject.toml`. Сучасні інструменти на кшталт лінтерів, форматерів і тестових раннерів також дедалі частіше читають власну конфігурацію з `pyproject.toml`, а стандартизована таблиця `[dependency-groups]` (PEP 735) тепер дозволяє проєкту оголошувати групи залежностей лише для розробки — тести, лінтинг, документацію — теж там.",
      "strongAnswerSignalsUk": ["стандартизований файл конфігурації для системи збирання й метаданих (PEP 518/621)", "setup.py й Setuptools не застарілі — застаріле лише пряме виконання setup.py як команди", "[dependency-groups] (PEP 735) тепер дозволяє оголошувати dev-залежності і в pyproject.toml"],
      "example": "A single `pyproject.toml` can declare the build backend under `[build-system]`, the package metadata and dependencies under `[project]`, and pytest or ruff configuration under `[tool.pytest.ini_options]` or `[tool.ruff]`, all in one file.",
      "exampleUk": "Єдиний `pyproject.toml` може оголошувати бекенд збирання в `[build-system]`, метадані пакета й залежності в `[project]`, а конфігурацію pytest чи ruff — у `[tool.pytest.ini_options]` чи `[tool.ruff]`, усе в одному файлі.",
      "tags": ["packaging", "tooling"],
      "sourceIds": ["packaging-guide"]
    },
    {
      "id": "py-pkg-wheel-vs-sdist",
      "level": "Senior",
      "prevalence": "Occasional",
      "category": "Modules, packaging and environments",
      "kind": "Theory",
      "question": "What is the difference between a source distribution (sdist) and a wheel, and why do wheels install faster?",
      "shortAnswer": "A source distribution (`.tar.gz`) contains a package's raw source code plus build instructions, and installing it may require running a build step, including compiling any C extensions, on the installing machine at install time. A wheel (`.whl`) is a pre-built, ready-to-install archive — for a pure-Python package it just needs unzipping into `site-packages`, and for a package with compiled extensions, a platform-specific wheel already contains the compiled binary for that OS and Python version, so `pip` never needs a working C compiler on the installing machine to succeed.",
      "strongAnswerSignals": ["sdist ships source plus build instructions, may compile at install time", "wheel is pre-built and ready to install, often just an unzip", "platform-specific wheels avoid needing a compiler on the install machine"],
      "questionUk": "Яка різниця між дистрибутивом вихідного коду (sdist) і wheel, і чому wheel встановлюються швидше?",
      "shortAnswerUk": "Дистрибутив вихідного коду (`.tar.gz`) містить сирий вихідний код пакета плюс інструкції збирання, і його встановлення може вимагати запуску кроку збирання, включно з компіляцією будь-яких C-розширень, на машині, що встановлює, у момент встановлення. Wheel (`.whl`) — це попередньо зібраний, готовий до встановлення архів — для чисто-Python пакета його достатньо розпакувати в `site-packages`, а для пакета зі скомпільованими розширеннями специфічний для платформи wheel уже містить скомпільований бінарний файл для цієї ОС і версії Python, тож `pip` ніколи не потребує робочого компілятора C на машині, що встановлює, для успіху.",
      "strongAnswerSignalsUk": ["sdist постачає джерело плюс інструкції збирання, може компілювати при встановленні", "wheel попередньо зібраний і готовий до встановлення, часто просто розпакування", "специфічні для платформи wheel уникають потреби в компіляторі на машині встановлення"],
      "example": "Installing a NumPy wheel takes seconds because a precompiled binary matching your OS and Python version is downloaded, while building NumPy from its sdist on a machine without the right compiler toolchain can fail outright.",
      "exampleUk": "Встановлення wheel для NumPy займає секунди, бо завантажується скомпільований бінарний файл, що відповідає вашій ОС і версії Python, тоді як збирання NumPy з його sdist на машині без потрібного набору інструментів компіляції може взагалі провалитися.",
      "tags": ["packaging", "wheel"],
      "sourceIds": ["packaging-guide"]
    },
    {
      "id": "py-pkg-relative-vs-absolute-imports",
      "level": "Middle",
      "prevalence": "Common",
      "category": "Modules, packaging and environments",
      "kind": "Practical",
      "question": "What is the difference between an absolute import and a relative import, and when does a relative import fail?",
      "shortAnswer": "An absolute import (`from mypackage.utils import helper`) spells out the full path from a top-level package, and works regardless of which module does the importing. A relative import (`from .utils import helper` or `from ..other import thing`) is expressed relative to the importing module's own position inside its package, using leading dots to mean \"this package\" or \"a parent package\". Relative imports only work inside a proper package (a module that Python knows has a `__package__`); running a script directly with `python module.py` instead of `python -m package.module` sets `__package__` to an empty context, and any relative import inside that script then fails with an `ImportError`.",
      "strongAnswerSignals": ["absolute imports spell out the full path from a top-level package", "relative imports use leading dots relative to the importing module's package position", "relative imports fail when a module is run directly instead of as part of a package"],
      "questionUk": "Яка різниця між абсолютним і відносним імпортом, і коли відносний імпорт зазнає невдачі?",
      "shortAnswerUk": "Абсолютний імпорт (`from mypackage.utils import helper`) прописує повний шлях від пакета верхнього рівня і працює незалежно від того, який модуль виконує імпорт. Відносний імпорт (`from .utils import helper` чи `from ..other import thing`) виражається відносно позиції модуля, що імпортує, всередині власного пакета, використовуючи провідні крапки для позначення «цей пакет» чи «батьківський пакет». Відносні імпорти працюють лише всередині справжнього пакета (модуля, для якого Python знає `__package__`); запуск скрипта напряму через `python module.py` замість `python -m package.module` встановлює `__package__` у порожній контекст, і будь-який відносний імпорт усередині цього скрипта тоді зазнає невдачі з `ImportError`.",
      "strongAnswerSignalsUk": ["абсолютний імпорт прописує повний шлях від пакета верхнього рівня", "відносний імпорт використовує провідні крапки відносно позиції модуля в пакеті", "відносний імпорт зазнає невдачі, коли модуль запущено напряму, а не як частину пакета"],
      "example": "Running `python mypackage/module.py` directly makes any `from . import sibling` inside `module.py` raise `ImportError: attempted relative import with no known parent package`, while `python -m mypackage.module` works correctly.",
      "exampleUk": "Запуск `python mypackage/module.py` напряму призводить до того, що будь-який `from . import sibling` усередині `module.py` викликає `ImportError: attempted relative import with no known parent package`, тоді як `python -m mypackage.module` працює коректно.",
      "tags": ["imports", "packages", "gotchas"],
      "sourceIds": ["python-docs-import-system"]
    },
    {
      "id": "py-pkg-editable-installs",
      "level": "Senior",
      "prevalence": "Occasional",
      "category": "Modules, packaging and environments",
      "kind": "Practical",
      "question": "What does `pip install -e .` (an editable install) do, and why is it useful during local development?",
      "shortAnswer": "An editable install registers a package in the current environment without copying its files into `site-packages`; instead it exposes the project's source directory to the environment through a backend-defined mechanism (PEP 660, typically a small finder or path hook rather than a literal symlink), so an ordinary Python source-code change typically takes effect the next time the package is imported, without a reinstall step. This is the standard workflow for developing a library alongside an application that depends on it. The exact mechanics are backend-defined rather than one universal guarantee, though: adding a new module, changing build-time metadata like console-script entry points, or touching anything that requires a compiled extension to be rebuilt can still require rerunning the editable install, so \"every change takes effect instantly\" is a useful approximation rather than an absolute rule.",
      "strongAnswerSignals": ["exposes the source directory via a backend-defined mechanism (PEP 660), not necessarily a literal symlink", "plain source changes typically take effect without reinstalling", "new modules, metadata changes, or extension rebuilds can still require rerunning the editable install"],
      "questionUk": "Що робить `pip install -e .` (редаговане встановлення), і чому це корисно під час локальної розробки?",
      "shortAnswerUk": "Редаговане встановлення реєструє пакет у поточному середовищі, не копіюючи його файли в `site-packages`; натомість воно виставляє вихідний каталог проєкту середовищу через механізм, визначений бекендом (PEP 660, зазвичай невеликий файндер чи хук шляху, а не буквальне символічне посилання), тож звичайна зміна вихідного коду Python зазвичай набуває чинності наступного разу, коли пакет імпортують, без кроку перевстановлення. Це стандартний робочий процес для розробки бібліотеки поряд із застосунком, що від неї залежить. Точна механіка визначається бекендом, а не є єдиною універсальною гарантією: додавання нового модуля, зміна метаданих часу збирання на кшталт точок входу консольних скриптів чи торкання чогось, що вимагає перезбирання скомпільованого розширення, все ще можуть вимагати повторного запуску редагованого встановлення, тож «кожна зміна набуває чинності миттєво» — корисне наближення, а не абсолютне правило.",
      "strongAnswerSignalsUk": ["виставляє вихідний каталог через механізм бекенду (PEP 660), не обов'язково буквальне символічне посилання", "звичайні зміни вихідного коду зазвичай набувають чинності без перевстановлення", "нові модулі, зміни метаданих чи перезбирання розширень все ще можуть вимагати повторного встановлення"],
      "example": "`pip install -e ./mylib` inside an application's virtual environment lets a developer edit `mylib`'s source directly and see the change reflected in the running application on the very next import, without reinstalling.",
      "exampleUk": "`pip install -e ./mylib` усередині віртуального середовища застосунку дозволяє розробнику редагувати вихідний код `mylib` напряму й бачити зміну відображеною в застосунку, що працює, вже при наступному імпорті, без перевстановлення.",
      "tags": ["packaging", "development-workflow"],
      "sourceIds": ["packaging-guide"]
    }
  ]
}
