{
  "questions": [
    {
      "id": "py-conc-gil-what-and-why",
      "level": "Middle",
      "prevalence": "Very common",
      "category": "Concurrency and parallelism",
      "kind": "Theory",
      "question": "What is the Global Interpreter Lock (GIL), and why does it exist in the default CPython build?",
      "shortAnswer": "The GIL is a single mutex inside the default CPython interpreter that allows only one thread to execute Python bytecode at a time, even on a machine with multiple CPU cores. It exists because CPython's memory management, particularly reference counting used for garbage collection, is not thread-safe by default, and the GIL was a simple, effective way to avoid data races on every object's reference count without requiring fine-grained locking throughout the interpreter. The practical consequence is that pure-Python, CPU-bound multi-threaded code does not get real parallel speedup on multiple cores, though I/O-bound threads still benefit because the GIL is released during blocking I/O calls.",
      "strongAnswerSignals": ["a single mutex allowing only one thread to run Python bytecode at a time", "exists because reference counting is not thread-safe without it", "CPU-bound threads don't parallelize, I/O-bound threads still benefit"],
      "questionUk": "Що таке глобальне блокування інтерпретатора (GIL), і чому воно існує в типовій збірці CPython?",
      "shortAnswerUk": "GIL — це єдиний м'ютекс усередині типового інтерпретатора CPython, що дозволяє лише одному потоку виконувати байткод Python одночасно, навіть на машині з кількома ядрами CPU. Він існує, бо керування пам'яттю CPython, зокрема підрахунок посилань, що використовується для збирання сміття, за замовчуванням не є потокобезпечним, і GIL був простим та ефективним способом уникнути гонок даних на лічильнику посилань кожного об'єкта без потреби в дрібнозернистому блокуванні по всьому інтерпретатору. Практичний наслідок — чистий Python-код, прив'язаний до CPU, у багатопотоковому виконанні не отримує реального паралельного прискорення на кількох ядрах, хоча потоки, прив'язані до вводу-виводу, все ще виграють, бо GIL звільняється під час блокуючих операцій вводу-виводу.",
      "strongAnswerSignalsUk": ["єдиний м'ютекс, що дозволяє лише одному потоку виконувати байткод", "існує, бо підрахунок посилань не потокобезпечний без нього", "CPU-прив'язані потоки не паралелізуються, I/O-прив'язані все ще виграють"],
      "example": "Four Python threads each doing pure CPU-bound number crunching on a 4-core machine typically run barely faster than one thread, while four threads each waiting on network requests can genuinely overlap their waiting time.",
      "exampleUk": "Чотири потоки Python, кожен з яких виконує чисту CPU-обчислювальну роботу на 4-ядерній машині, зазвичай працюють ледь швидше за один потік, тоді як чотири потоки, що чекають на мережеві запити, можуть реально перекривати час очікування.",
      "tags": ["gil", "concurrency", "cpython-internals"],
      "sourceIds": ["python-docs-free-threading", "pep-703"]
    },
    {
      "id": "py-conc-threading-vs-multiprocessing",
      "level": "Middle",
      "prevalence": "Very common",
      "category": "Concurrency and parallelism",
      "kind": "Design",
      "question": "How do you decide between `threading` and `multiprocessing` for a given workload?",
      "shortAnswer": "Use `threading` for I/O-bound work — network calls, file access, database queries — where most of the time is spent waiting, because in the default GIL-enabled CPython build the GIL is released during blocking I/O and threads are lightweight to create with shared memory. Use `multiprocessing` for CPU-bound work — heavy computation, image processing, data crunching — because each process gets its own Python interpreter and its own GIL, giving genuine parallel execution across cores, at the cost of higher startup overhead, no shared memory by default, and the requirement that data passed between processes be picklable. This split is the usual heuristic for the default CPython build, not an absolute rule: Python 3.14's officially supported free-threaded build lets CPU-bound Python threads run genuinely in parallel too, and many native extensions (NumPy and other C-accelerated libraries) release the GIL during their own CPU-heavy work even on the default build.",
      "strongAnswerSignals": ["threading for I/O-bound work, GIL releases during blocking calls in the default build", "multiprocessing for CPU-bound work, separate interpreter and GIL per process", "the split is a default-build heuristic, not absolute — free-threaded 3.14 and GIL-releasing extensions are exceptions"],
      "questionUk": "Як обрати між `threading` та `multiprocessing` для конкретного навантаження?",
      "shortAnswerUk": "Використовуйте `threading` для роботи, прив'язаної до вводу-виводу — мережеві виклики, доступ до файлів, запити до бази даних — де більшість часу витрачається на очікування, бо в типовій збірці CPython з GIL він звільняється під час блокуючого вводу-виводу, а потоки легкі у створенні зі спільною пам'яттю. Використовуйте `multiprocessing` для роботи, прив'язаної до CPU — важкі обчислення, обробка зображень, обробка даних — бо кожен процес отримує власний інтерпретатор Python і власний GIL, даючи справжнє паралельне виконання на кількох ядрах, ціною вищих накладних витрат на запуск, відсутності спільної пам'яті за замовчуванням і вимоги, щоб дані, що передаються між процесами, були придатні для pickle. Цей поділ — типова евристика для стандартної збірки CPython, а не абсолютне правило: офіційно підтримувана free-threaded збірка Python 3.14 дозволяє й потокам, прив'язаним до CPU, справді паралелізуватися, а багато нативних розширень (NumPy та інші C-прискорені бібліотеки) звільняють GIL під час власної CPU-важкої роботи навіть у стандартній збірці.",
      "strongAnswerSignalsUk": ["threading для I/O-роботи, GIL звільняється при блокуючих викликах у стандартній збірці", "multiprocessing для CPU-роботи, окремий інтерпретатор і GIL на процес", "поділ — евристика для стандартної збірки, не абсолют — free-threaded 3.14 і розширення, що звільняють GIL, є винятками"],
      "example": "A web scraper making thousands of HTTP requests fits `threading` (or better, `asyncio`); resizing ten thousand images on disk fits `multiprocessing` to actually use all CPU cores.",
      "exampleUk": "Веб-скрапер, що робить тисячі HTTP-запитів, підходить для `threading` (або краще, `asyncio`); зміна розміру десяти тисяч зображень на диску підходить для `multiprocessing`, щоб реально задіяти всі ядра CPU.",
      "tags": ["threading", "multiprocessing", "design"],
      "sourceIds": ["python-docs-threading", "python-docs-multiprocessing", "toptal-python"]
    },
    {
      "id": "py-conc-asyncio-event-loop-coroutines",
      "level": "Senior",
      "prevalence": "Very common",
      "category": "Concurrency and parallelism",
      "kind": "Theory",
      "question": "How does the asyncio event loop achieve concurrency with a single thread, and what makes a function a coroutine?",
      "shortAnswer": "A function defined with `async def` is a coroutine function; calling it returns a coroutine object that does nothing until it is awaited or scheduled as a task. The event loop runs one coroutine at a time on a single thread, and whenever that coroutine hits an `await` on something that would block (like network I/O), it voluntarily yields control back to the loop, which then runs another ready coroutine in the meantime. This is cooperative multitasking: concurrency comes from many coroutines taking turns at `await` points, not from parallel execution, so a coroutine that never awaits (pure CPU work) blocks the entire event loop for everyone.",
      "strongAnswerSignals": ["async def creates a coroutine function, calling it just returns an object", "the loop runs one coroutine at a time, switching at await points", "cooperative, not parallel — a non-awaiting coroutine blocks everything"],
      "questionUk": "Як цикл подій asyncio досягає конкурентності одним потоком, і що робить функцію корутиною?",
      "shortAnswerUk": "Функція, визначена через `async def`, — функція-корутина; її виклик повертає об'єкт-корутину, який нічого не робить, доки його не очікують (await) чи не заплановано як задачу. Цикл подій виконує одну корутину за раз в одному потоці, і щоразу, коли ця корутина натрапляє на `await` чогось, що заблокувало б (як мережевий ввід-вивід), вона добровільно повертає керування циклу, який тоді виконує іншу готову корутину тим часом. Це кооперативна багатозадачність: конкурентність виникає з того, що багато корутин по черзі проходять точки `await`, а не з паралельного виконання, тож корутина, яка ніколи не робить await (чиста обчислювальна робота), блокує весь цикл подій для всіх.",
      "strongAnswerSignalsUk": ["async def створює функцію-корутину, виклик просто повертає об'єкт", "цикл виконує одну корутину за раз, перемикається в точках await", "кооперативно, не паралельно — некорутина, що не робить await, блокує все"],
      "example": "`async def fetch_all(urls):\\n    return await asyncio.gather(*(fetch(u) for u in urls))` can have hundreds of `fetch` coroutines conceptually \"in flight\" at once, all sharing one thread, because each `await` point lets others make progress.",
      "exampleUk": "`async def fetch_all(urls):\\n    return await asyncio.gather(*(fetch(u) for u in urls))` може мати сотні корутин `fetch` концептуально «в польоті» одночасно, всі в одному потоці, бо кожна точка `await` дозволяє іншим прогресувати.",
      "tags": ["asyncio", "coroutines", "event-loop"],
      "sourceIds": ["python-docs-asyncio", "pep-492"]
    },
    {
      "id": "py-conc-race-conditions-and-locks",
      "level": "Senior",
      "prevalence": "Common",
      "category": "Concurrency and parallelism",
      "kind": "Troubleshooting",
      "question": "How can a race condition still occur in Python threads despite the GIL, and how does `threading.Lock` prevent it?",
      "shortAnswer": "A compound operation like `counter += 1` is really a read-modify-write sequence (load the old value, add one, store the new value) rather than one indivisible step, so two threads can each read the same old value before either writes back the incremented result, and one increment is silently lost — a classic unsynchronized race regardless of exactly how that sequence is implemented internally. In the default GIL-enabled CPython build, only one thread executes Python bytecode at a time, but the interpreter can still switch which thread is running between the individual steps of that sequence, so the compound operation as a whole is not protected. `threading.Lock` (used as `with lock:`) ensures only one thread can execute the protected block at a time, making the whole read-modify-write sequence effectively atomic from the perspective of other threads that respect the same lock — and this fix is necessary regardless of whether a GIL is present, which matters because Python 3.14's free-threaded build removes the GIL entirely and needs exactly this kind of explicit locking even more than before.",
      "strongAnswerSignals": ["compound read-modify-write operations like += are not one indivisible step", "the interpreter can switch threads between the individual steps, losing an update", "Lock serializes access so the sequence is effectively atomic, regardless of whether a GIL is present"],
      "questionUk": "Як стан гонки все ще може виникнути у потоках Python попри GIL, і як `threading.Lock` йому запобігає?",
      "shortAnswerUk": "Складена операція на кшталт `counter += 1` насправді є послідовністю читання-зміни-запису (завантажити старе значення, додати одиницю, зберегти нове), а не одним неподільним кроком, тож два потоки можуть обидва прочитати те саме старе значення до того, як хтось із них запише збільшений результат, і одне збільшення тихо втрачається — класична несинхронізована гонка незалежно від того, як саме ця послідовність реалізована внутрішньо. У типовій збірці CPython з GIL лише один потік виконує байткод Python за раз, але інтерпретатор все ще може перемкнути, який потік виконується, між окремими кроками цієї послідовності, тож складена операція в цілому не захищена. `threading.Lock` (використаний як `with lock:`) гарантує, що лише один потік може виконувати захищений блок за раз, роблячи всю послідовність читання-зміни-запису фактично атомарною з погляду інших потоків, що поважають те саме блокування — і це виправлення потрібне незалежно від наявності GIL, що важливо, бо free-threaded збірка Python 3.14 повністю прибирає GIL і потребує саме такого явного блокування ще більше, ніж раніше.",
      "strongAnswerSignalsUk": ["складені операції читання-зміни-запису на кшталт += — не один неподільний крок", "інтерпретатор може перемкнути потоки між окремими кроками, втративши оновлення", "Lock серіалізує доступ, роблячи послідовність атомарною незалежно від наявності GIL"],
      "example": "Ten threads each running `counter += 1` a thousand times without a lock commonly finish with `counter` well below 10,000; wrapping the increment in `with lock: counter += 1` fixes it reliably.",
      "exampleUk": "Десять потоків, кожен з яких виконує `counter += 1` тисячу разів без блокування, зазвичай завершуються зі значенням `counter` значно нижчим за 10 000; обгортання інкременту в `with lock: counter += 1` надійно виправляє це.",
      "tags": ["threading", "race-conditions", "locks"],
      "sourceIds": ["python-docs-threading"]
    },
    {
      "id": "py-conc-deadlock-causes-avoidance",
      "level": "Senior",
      "prevalence": "Occasional",
      "category": "Concurrency and parallelism",
      "kind": "Troubleshooting",
      "question": "What causes a deadlock between two threads, and what are the standard ways to avoid it?",
      "shortAnswer": "A classic deadlock happens when thread A holds lock 1 and waits for lock 2, while thread B holds lock 2 and waits for lock 1 at the same time; neither can proceed and both wait forever. The standard prevention strategies are: always acquire multiple locks in the same globally agreed order across all code paths, use a timeout on lock acquisition (`lock.acquire(timeout=5)`) so a stuck thread eventually gives up and can recover or report an error, or restructure the code to avoid needing to hold more than one lock at a time in the first place.",
      "strongAnswerSignals": ["two threads each hold one lock and wait on the other's lock, forever", "prevention: consistent global lock ordering across all code paths", "alternatives: acquisition timeouts, or avoid holding multiple locks at once"],
      "questionUk": "Що спричиняє взаємне блокування (deadlock) між двома потоками, і які стандартні способи його уникнути?",
      "shortAnswerUk": "Класичне взаємне блокування трапляється, коли потік A тримає блокування 1 і чекає на блокування 2, тоді як потік B тримає блокування 2 і чекає на блокування 1 в той самий час; жоден не може продовжити, й обидва чекають вічно. Стандартні стратегії запобігання: завжди захоплювати кілька блокувань у тому самому глобально узгодженому порядку в усіх шляхах коду, використовувати тайм-аут на захоплення блокування (`lock.acquire(timeout=5)`), щоб застряглий потік врешті здався й міг відновитися чи повідомити про помилку, або перебудувати код так, щоб взагалі не тримати більше одного блокування одночасно.",
      "strongAnswerSignalsUk": ["два потоки, кожен тримає одне блокування й чекає на інше, вічно", "запобігання: узгоджений глобальний порядок захоплення блокувань", "альтернативи: тайм-аути захоплення, уникання кількох блокувань одночасно"],
      "example": "`transfer(account_a, account_b)` locking `account_a` then `account_b`, while a concurrent `transfer(account_b, account_a)` locks `account_b` then `account_a`, can deadlock; always locking accounts in a fixed order (e.g. by account id) avoids it.",
      "exampleUk": "`transfer(account_a, account_b)`, що блокує `account_a`, а потім `account_b`, тоді як паралельний `transfer(account_b, account_a)` блокує `account_b`, а потім `account_a`, може призвести до взаємного блокування; блокування рахунків у фіксованому порядку (наприклад, за id рахунку) уникає цього.",
      "tags": ["threading", "deadlock", "locks"],
      "sourceIds": ["python-docs-threading"]
    },
    {
      "id": "py-conc-concurrent-futures-pools",
      "level": "Middle",
      "prevalence": "Common",
      "category": "Concurrency and parallelism",
      "kind": "Practical",
      "question": "What does the `concurrent.futures` module provide, and how do `ThreadPoolExecutor` and `ProcessPoolExecutor` differ in usage?",
      "shortAnswer": "`concurrent.futures` gives a unified, higher-level API for submitting callables to a pool of workers and getting back `Future` objects that represent results still being computed, replacing a lot of manual thread- or process-management boilerplate. `ThreadPoolExecutor` and `ProcessPoolExecutor` share the same core `Executor`/`Future` interface — `submit()`, `map()`, and `as_completed()` — so switching between thread-based and process-based execution for a given workload is often a small change, though each executor keeps its own constraints that the shared interface does not remove (picklable arguments and return values for processes; no true CPU parallelism for threads on a default GIL-enabled interpreter). Whether threads actually gain CPU parallelism depends on the interpreter build: the default GIL-enabled CPython build does not give threads true parallel CPU execution, but Python 3.14's officially supported free-threaded build does.",
      "strongAnswerSignals": ["unified API returning Future objects from a worker pool", "ThreadPoolExecutor and ProcessPoolExecutor share the core Executor/Future interface but keep their own constraints", "CPU parallelism for threads depends on the interpreter build — default GIL build vs free-threaded 3.14"],
      "questionUk": "Що надає модуль `concurrent.futures`, і чим відрізняється використання `ThreadPoolExecutor` і `ProcessPoolExecutor`?",
      "shortAnswerUk": "`concurrent.futures` дає уніфікований API вищого рівня для передачі викличних об'єктів у пул воркерів і отримання назад об'єктів `Future`, що представляють результати, які ще обчислюються, замінюючи багато ручного шаблонного коду керування потоками чи процесами. `ThreadPoolExecutor` і `ProcessPoolExecutor` мають той самий базовий інтерфейс `Executor`/`Future` — `submit()`, `map()` та `as_completed()` — тож перехід між виконанням на основі потоків і процесів для конкретного навантаження часто невелика зміна, хоча кожен виконавець зберігає власні обмеження, яких спільний інтерфейс не усуває (придатні для pickle аргументи й значення, що повертаються, для процесів; відсутність справжньої CPU-паралельності для потоків у стандартному інтерпретаторі з GIL). Чи потоки реально отримують CPU-паралельність, залежить від збірки інтерпретатора: типова збірка CPython з GIL не дає потокам справжнього паралельного виконання CPU, а офіційно підтримувана free-threaded збірка Python 3.14 — дає.",
      "strongAnswerSignalsUk": ["уніфікований API, що повертає об'єкти Future з пулу воркерів", "ThreadPoolExecutor і ProcessPoolExecutor мають спільний базовий інтерфейс, але власні обмеження", "CPU-паралельність потоків залежить від збірки інтерпретатора — стандартна з GIL проти free-threaded 3.14"],
      "example": "`with ThreadPoolExecutor(max_workers=8) as pool:\\n    results = list(pool.map(fetch_url, urls))` becomes CPU-parallel just by swapping `ThreadPoolExecutor` for `ProcessPoolExecutor`, as long as `fetch_url` and its arguments are picklable.",
      "exampleUk": "`with ThreadPoolExecutor(max_workers=8) as pool:\\n    results = list(pool.map(fetch_url, urls))` стає паралельним за CPU просто заміною `ThreadPoolExecutor` на `ProcessPoolExecutor`, якщо `fetch_url` і його аргументи придатні для pickle.",
      "tags": ["concurrent-futures", "threading", "multiprocessing"],
      "sourceIds": ["python-docs-threading", "python-docs-multiprocessing"]
    },
    {
      "id": "py-conc-free-threading-python-3-14",
      "level": "Senior",
      "prevalence": "Occasional",
      "category": "Concurrency and parallelism",
      "kind": "Theory",
      "question": "What is free-threaded Python, and what is its current status as of Python 3.14?",
      "shortAnswer": "Free-threaded Python is a build of CPython, proposed by PEP 703, that removes the GIL entirely so multiple threads can execute Python bytecode truly in parallel on multiple cores, using more fine-grained internal locking and a different memory management scheme instead. PEP 779 defined the criteria for calling this build officially supported, and Python 3.14 met those criteria, making free-threading officially supported for the first time — but it remains an opt-in alternative build, not the default, because it carries a single-threaded performance cost and many C-extension packages are not yet fully compatible with it.",
      "strongAnswerSignals": ["PEP 703 build removes the GIL for true parallel bytecode execution", "PEP 779 set the criteria; 3.14 made it officially supported", "still opt-in, not default, due to single-thread overhead and extension compatibility"],
      "questionUk": "Що таке безблокувальний (free-threaded) Python, і який його поточний статус станом на Python 3.14?",
      "shortAnswerUk": "Free-threaded Python — це збірка CPython, запропонована PEP 703, що повністю прибирає GIL, тож кілька потоків можуть виконувати байткод Python справді паралельно на кількох ядрах, використовуючи натомість дрібнозернистіше внутрішнє блокування та іншу схему керування пам'яттю. PEP 779 визначив критерії для офіційної підтримки цієї збірки, і Python 3.14 відповів цим критеріям, зробивши free-threading офіційно підтримуваним вперше — але це залишається опційною альтернативною збіркою, а не типовою, бо вона несе витрати продуктивності в однопотоковому режимі, і багато пакетів з C-розширеннями ще не повністю сумісні з нею.",
      "strongAnswerSignalsUk": ["збірка PEP 703 прибирає GIL для справжнього паралельного виконання байткоду", "PEP 779 визначив критерії; 3.14 зробив офіційно підтримуваним", "все ще опційно, не типово, через витрати в однопотоковому режимі й сумісність розширень"],
      "example": "Running `python3.14t` (the free-threaded build) lets a CPU-bound thread pool actually scale across cores, something impossible on the standard GIL build, but a third-party extension without free-threading support may re-enable the GIL automatically when imported.",
      "exampleUk": "Запуск `python3.14t` (free-threaded збірки) дозволяє пулу потоків, прив'язаних до CPU, реально масштабуватися на кілька ядер — те, що неможливо в стандартній збірці з GIL, але стороннє розширення без підтримки free-threading може автоматично повторно увімкнути GIL при імпорті.",
      "tags": ["gil", "free-threading", "python-3-14"],
      "sourceIds": ["pep-703", "pep-779", "python-docs-free-threading"]
    },
    {
      "id": "py-conc-asyncio-gather-vs-wait",
      "level": "Senior",
      "prevalence": "Occasional",
      "category": "Concurrency and parallelism",
      "kind": "Practical",
      "question": "How does `asyncio.gather()` differ from `asyncio.wait()` when running multiple coroutines concurrently?",
      "shortAnswer": "`asyncio.gather(*coros)` schedules all coroutines to run concurrently and returns their results as a list in the same order the coroutines were passed in, once all complete. With the default `return_exceptions=False`, the first coroutine to fail has its exception propagated to the caller immediately, but the other coroutines submitted to that same `gather()` call are not automatically cancelled — they keep running in the background unless something else cancels them, such as the caller cancelling the `gather()` call itself. This differs from `asyncio.TaskGroup` (Python 3.11+), which does cancel sibling tasks once one of them fails with anything other than a cancellation. `asyncio.wait(tasks)` instead returns two sets, `(done, pending)`, without raising exceptions itself and without preserving input order, giving finer control over partial completion, custom timeout handling, and returning as soon as the first task finishes if configured with `return_when=FIRST_COMPLETED`.",
      "strongAnswerSignals": ["gather returns ordered results; by default the first exception propagates but sibling coroutines are not auto-cancelled", "TaskGroup, unlike gather, does cancel sibling tasks on a non-cancellation failure", "wait returns (done, pending) sets without raising, order not preserved"],
      "questionUk": "Чим `asyncio.gather()` відрізняється від `asyncio.wait()` при паралельному виконанні кількох корутин?",
      "shortAnswerUk": "`asyncio.gather(*coros)` планує всі корутини для паралельного виконання й повертає їхні результати у вигляді списку в тому самому порядку, в якому корутини були передані, коли всі завершаться. З типовим `return_exceptions=False` виняток першої корутини, що зазнала невдачі, одразу поширюється до викликача, але інші корутини, передані в той самий виклик `gather()`, не скасовуються автоматично — вони продовжують виконуватись у фоні, якщо щось інше їх не скасує, наприклад якщо викликач скасує сам виклик `gather()`. Це відрізняється від `asyncio.TaskGroup` (Python 3.11+), яка справді скасовує сусідні задачі, щойно одна з них падає з чимось, відмінним від скасування. `asyncio.wait(tasks)` натомість повертає два набори, `(done, pending)`, не кидаючи винятків самостійно й не зберігаючи порядок введення, даючи тонший контроль над частковим завершенням, власною обробкою тайм-ауту й поверненням щойно перша задача завершиться, якщо налаштовано з `return_when=FIRST_COMPLETED`.",
      "strongAnswerSignalsUk": ["gather повертає впорядковані результати; за замовчуванням перший виняток поширюється, але сусідні корутини не скасовуються автоматично", "TaskGroup, на відміну від gather, скасовує сусідні задачі при невдачі, що не є скасуванням", "wait повертає набори (done, pending) без кидання винятків, порядок не зберігається"],
      "example": "`results = await asyncio.gather(fetch(1), fetch(2), fetch(3))` gives `[result1, result2, result3]` in that exact order, while `done, pending = await asyncio.wait(tasks, return_when=asyncio.FIRST_COMPLETED)` returns as soon as any single task finishes.",
      "exampleUk": "`results = await asyncio.gather(fetch(1), fetch(2), fetch(3))` дає `[result1, result2, result3]` саме в такому порядку, тоді як `done, pending = await asyncio.wait(tasks, return_when=asyncio.FIRST_COMPLETED)` повертається, щойно будь-яка одна задача завершиться.",
      "tags": ["asyncio", "coroutines"],
      "sourceIds": ["python-docs-asyncio"]
    },
    {
      "id": "py-conc-thread-safety-of-builtins",
      "level": "Senior",
      "prevalence": "Occasional",
      "category": "Concurrency and parallelism",
      "kind": "Theory",
      "question": "Are Python's built-in data structures like `list` and `dict` thread-safe?",
      "shortAnswer": "Individual built-in operations that map to a single bytecode instruction, like `list.append()` or a single dict key assignment, are effectively atomic in the default GIL-enabled CPython build and will not corrupt the underlying data structure — though this has never been a formal language-level guarantee, only an incidental consequence of how the reference implementation happens to work. In Python 3.14's free-threaded build, the GIL is gone entirely, and built-in containers instead rely on their own internal fine-grained locking to stay structurally safe, so the mechanism differs even though simple single-operation safety still generally holds. Either way, this does not make the type \"thread-safe\" in a general sense: any sequence of multiple operations that needs to happen as one logical unit, such as \"check if a key exists, then insert if not,\" is not atomic as a whole and is still subject to race conditions between the individual steps. The recommended practice is to identify the invariant your code actually needs and protect it with explicit synchronization — a `Lock`, a queue, or clear single-owner access — rather than relying on any interpreter's incidental atomicity.",
      "strongAnswerSignals": ["single-instruction-style operations are effectively atomic in the default GIL build, but that was never a formal guarantee", "free-threaded 3.14 uses its own internal locking instead of the GIL for that safety", "multi-step logical operations are never atomic as a whole; protect real invariants with explicit locks"],
      "questionUk": "Чи є вбудовані структури даних Python, як-от `list` і `dict`, потокобезпечними?",
      "shortAnswerUk": "Окремі вбудовані операції, що відповідають одній інструкції байткоду, як `list.append()` чи одне присвоєння ключа словника, фактично атомарні в типовій збірці CPython з GIL і не пошкодять базову структуру даних — хоча це ніколи не було формальною гарантією на рівні мови, лише випадковим наслідком того, як працює еталонна реалізація. У free-threaded збірці Python 3.14 GIL взагалі немає, і вбудовані контейнери натомість покладаються на власне внутрішнє дрібнозернисте блокування, щоб залишатися структурно безпечними, тож механізм відрізняється, хоча проста атомарність окремої операції загалом зберігається. У будь-якому разі це не робить тип «потокобезпечним» у загальному сенсі: будь-яка послідовність з кількох операцій, що має відбутися як одна логічна одиниця, наприклад «перевірити, чи існує ключ, потім вставити, якщо ні», не атомарна в цілому й все ще підвладна станам гонки між окремими кроками. Рекомендована практика — визначити реальний інваріант, потрібний коду, і захистити його явною синхронізацією: `Lock`, чергою чи чітким єдиним власником доступу, а не покладатися на випадкову атомарність будь-якого інтерпретатора.",
      "strongAnswerSignalsUk": ["однокрокові операції фактично атомарні в стандартній збірці з GIL, але це ніколи не було формальною гарантією", "free-threaded 3.14 використовує власне внутрішнє блокування замість GIL для цього", "багатокрокові логічні операції ніколи не атомарні в цілому; захищайте реальні інваріанти явними блокуваннями"],
      "example": "`if key not in cache: cache[key] = compute()` can still run `compute()` twice from two threads, because the check and the assignment are two separate atomic steps, not one.",
      "exampleUk": "`if key not in cache: cache[key] = compute()` все ще може виконати `compute()` двічі з двох потоків, бо перевірка й присвоєння — два окремі атомарні кроки, а не один.",
      "tags": ["threading", "race-conditions"],
      "sourceIds": ["python-docs-threading", "python-docs-datamodel"]
    },
    {
      "id": "py-conc-multiprocessing-pickling",
      "level": "Middle",
      "prevalence": "Common",
      "category": "Concurrency and parallelism",
      "kind": "Troubleshooting",
      "question": "Why must arguments and return values in `multiprocessing` code be picklable, and what commonly fails this requirement?",
      "shortAnswer": "Because each worker process has entirely separate memory from the parent, `multiprocessing` cannot share Python objects directly across the process boundary; instead it serializes (pickles) the target function, its arguments, and its return value to send them between processes, and deserializes them on the other side. Common things that fail to pickle include lambdas, locally-defined nested functions and classes, open file handles, database connections, and unpicklable third-party objects like some GUI or driver handles — these need to be replaced with module-level functions/classes or re-created inside the worker process instead of being passed in directly.",
      "strongAnswerSignals": ["separate process memory means objects must be serialized across the boundary", "multiprocessing pickles the function, arguments, and return value", "lambdas, local functions/classes, open handles commonly fail to pickle"],
      "questionUk": "Чому аргументи й значення, що повертаються, у коді `multiprocessing` мають бути придатними для pickle, і що часто не відповідає цій вимозі?",
      "shortAnswerUk": "Оскільки кожен робочий процес має повністю окрему пам'ять від батьківського, `multiprocessing` не може ділити об'єкти Python напряму через межу процесу; натомість він серіалізує (pickle) цільову функцію, її аргументи й значення, що повертається, щоб передати їх між процесами, і десеріалізує на іншому боці. Поширені речі, що не проходять pickle: lambda, локально визначені вкладені функції та класи, відкриті файлові дескриптори, підключення до бази даних та непридатні для pickle сторонні об'єкти, як-от деякі дескриптори GUI чи драйверів — їх потрібно замінити функціями/класами рівня модуля чи повторно створити всередині робочого процесу, а не передавати напряму.",
      "strongAnswerSignalsUk": ["окрема пам'ять процесу означає, що об'єкти треба серіалізувати через межу", "multiprocessing серіалізує функцію, аргументи й значення, що повертається", "lambda, локальні функції/класи, відкриті дескриптори часто не проходять pickle"],
      "example": "`pool.map(lambda x: x * 2, data)` raises a `PicklingError` under `multiprocessing`, while replacing the lambda with a module-level `def double(x): return x * 2` works correctly.",
      "exampleUk": "`pool.map(lambda x: x * 2, data)` викликає `PicklingError` під `multiprocessing`, тоді як заміна lambda на функцію рівня модуля `def double(x): return x * 2` працює коректно.",
      "tags": ["multiprocessing", "pickling", "gotchas"],
      "sourceIds": ["python-docs-multiprocessing"]
    },
    {
      "id": "py-conc-cpu-bound-vs-io-bound",
      "level": "Middle",
      "prevalence": "Common",
      "category": "Concurrency and parallelism",
      "kind": "Design",
      "question": "How do you tell whether a workload is CPU-bound or I/O-bound, and why does that classification drive the choice of concurrency model?",
      "shortAnswer": "A workload is CPU-bound if its runtime is dominated by computation the processor must actually perform — parsing, math, image transforms — and profiling shows high CPU utilization throughout. It is I/O-bound if most of its runtime is spent waiting on something external — a network response, disk access, another service — with the CPU sitting mostly idle during those waits. The classification matters because in the default GIL-enabled CPython build, threads and asyncio coroutines only help I/O-bound work, since the GIL prevents CPU-bound Python code on threads from running in true parallel; CPU-bound work on that build needs `multiprocessing`, or offloading to a C extension or native library that releases the GIL, to actually go faster on multiple cores. Python 3.14's officially supported free-threaded build changes this specifically for CPU-bound threads, letting them scale across cores without multiprocessing, though it remains an opt-in alternative build rather than the default.",
      "strongAnswerSignals": ["CPU-bound: runtime dominated by actual computation, high CPU use", "I/O-bound: runtime dominated by waiting on something external, CPU mostly idle", "on the default GIL build only multiprocessing/native code parallelizes CPU-bound work; free-threaded 3.14 is the exception"],
      "questionUk": "Як визначити, чи навантаження прив'язане до CPU, чи до вводу-виводу, і чому ця класифікація визначає вибір моделі конкурентності?",
      "shortAnswerUk": "Навантаження прив'язане до CPU, якщо його час виконання визначається обчисленнями, які процесор реально має виконати — розбір, математика, трансформації зображень, і профілювання показує стабільно високе використання CPU. Воно прив'язане до вводу-виводу, якщо більшість часу виконання витрачається на очікування чогось зовнішнього — мережевої відповіді, доступу до диска, іншого сервісу — з CPU здебільшого простоюючим під час цих очікувань. Ця класифікація важлива, бо в типовій збірці CPython з GIL потоки й корутини asyncio допомагають лише роботі, прив'язаній до I/O, оскільки GIL не дозволяє коду Python, прив'язаному до CPU, на потоках виконуватися справді паралельно; робота, прив'язана до CPU, у цій збірці потребує `multiprocessing` чи вивантаження в C-розширення/нативну бібліотеку, що звільняє GIL, щоб реально пришвидшитися на кількох ядрах. Офіційно підтримувана free-threaded збірка Python 3.14 змінює це саме для потоків, прив'язаних до CPU, дозволяючи їм масштабуватися на кілька ядер без multiprocessing, хоча вона залишається опційною альтернативною збіркою, а не типовою.",
      "strongAnswerSignalsUk": ["CPU-прив'язане: час визначається реальними обчисленнями, високе використання CPU", "I/O-прив'язане: час визначається очікуванням зовнішнього, CPU здебільшого простоює", "у стандартній збірці з GIL лише multiprocessing/нативний код паралелізують CPU-роботу; free-threaded 3.14 — виняток"],
      "example": "Parsing and validating a million JSON records in memory is CPU-bound and needs `multiprocessing` to speed up; downloading a million small files over HTTP is I/O-bound and speeds up dramatically with `asyncio` or a thread pool.",
      "exampleUk": "Розбір і валідація мільйона записів JSON у пам'яті — CPU-прив'язана робота, що потребує `multiprocessing` для пришвидшення; завантаження мільйона маленьких файлів через HTTP — I/O-прив'язана робота, що суттєво пришвидшується з `asyncio` чи пулом потоків.",
      "tags": ["concurrency", "design", "performance"],
      "sourceIds": ["python-docs-asyncio", "python-docs-multiprocessing"]
    }
  ]
}
