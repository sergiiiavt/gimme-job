{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "GimmeJob: Run in VS Code",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run local",
      "cwd": "${workspaceFolder}"
    }
  ]
}
