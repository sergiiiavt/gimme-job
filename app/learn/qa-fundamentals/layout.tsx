import { legacyReferenceMetadata } from "../../seo";

export const metadata = legacyReferenceMetadata("qa-fundamentals");

export default function QaFundamentalsLearningLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return children;
}
