import { learningSectionMetadata } from "../../seo";

export const metadata = learningSectionMetadata("data");

export default function DataLearningLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return children;
}
