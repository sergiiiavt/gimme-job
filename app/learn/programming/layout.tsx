import { legacyReferenceMetadata } from "../../seo";

export const metadata = legacyReferenceMetadata("programming");

export default function ProgrammingLearningLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return children;
}
