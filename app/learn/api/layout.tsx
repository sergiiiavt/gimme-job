import { learningSectionMetadata } from "../../seo";

export const metadata = learningSectionMetadata("api");

export default function ApiLearningLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return children;
}
